package api

import (
	"fmt"
	"testing"
)

func TestGetType(t *testing.T) {
	//color := GetPassType("c_conf", 0)
	//fmt.Println(color)
	c := map[string]string{"d": "a"}
	//WriteHash("c.json", c)
	fmt.Println(len(c["a"]))
}
